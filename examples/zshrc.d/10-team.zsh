# Project / team interactive-zsh — committed, shared with everyone on the repo.
# Runs after oh-my-zsh and feature drop-ins. Examples:

# alias gs='git status'
# alias k='kubectl'

# A project-standard right-prompt segment (kept off the left prompt so it
# composes with feature segments and doesn't fight a user's theme):
# RPROMPT='%F{yellow}[myproject]%f'"${RPROMPT}"

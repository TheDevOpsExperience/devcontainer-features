# Project-specific allowed domains — one per line, comments with #.
# Loaded at container start and added to the firewall allowlist.
# Use 'sudo allow-domain.sh <domain>' to temporarily allow a domain without restarting.
# Use 'sudo capture-dns.sh' to discover domains your tools are trying to reach.

# Example: a container registry you push to (see the buildkit feature README).
# registry.gitlab.com
# storage.googleapis.com

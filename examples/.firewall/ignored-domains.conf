# Domains to exclude from the DNS capture log — one per line.
# Supports wildcard prefix: *.example.com matches any subdomain of example.com.
# Useful for suppressing noisy VS Code / editor background traffic.
# *.vscode-cdn.net
# *.gallerycdn.vsassets.io
